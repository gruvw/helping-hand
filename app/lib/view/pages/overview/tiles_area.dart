import "package:flutter/material.dart";

class TilesArea extends StatelessWidget {
  const TilesArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
